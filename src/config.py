"""全局配置"""

from pathlib import Path

# 项目根目录
PROJECT_ROOT = Path(__file__).resolve().parent.parent

# 数据库文件路径
DATABASE_PATH = PROJECT_ROOT / "roko_monitor.db"

# 截图区域配置（相对坐标，0-1）
# 屏幕分辨率：1920*1080
CAPTURE_REGIONS = {
    "top_right": {      # 屏幕右上角单精灵（左上1677,40 右下1818,67）
        "x": 1677 / 1920,      # ≈ 0.8734
        "y": 40 / 1080,        # ≈ 0.0370
        "width": 141 / 1920,    # ≈ 0.0734 (1818-1677)
        "height": 27 / 1080,    # ≈ 0.0250 (67-40)
    },
    "team_list": {      # 配队列表（5个精灵框，第一个框左上806,366 右下975,394，间距107）
        "x": 806 / 1920,       # ≈ 0.4198
        "y": 366 / 1080,       # ≈ 0.3389
        "width": 169 / 1920,    # ≈ 0.0880 (975-806)
        "height": 456 / 1080,   # ≈ 0.4222 (单个高度28 + 4个间距*107)
    },
    "first_team": {    # 首发页面（6个精灵框，第一个框左上1545,236 右下1723,265，间隔100）
        "x": 1545 / 1920,       # ≈ 0.8047
        "y": 236 / 1080,        # ≈ 0.2185
        "width": 178 / 1920,    # ≈ 0.0927 (1723-1545)
        "height": 529 / 1080,   # ≈ 0.4907 (765-236)
    }
}

# OCR 识别配置
OCR_CONFIG = {
    "fuzzy_threshold": 70,    # 模糊匹配阈值（0-100）
    "auto_interval": 3000,    # 自动识别间隔（毫秒）
    "monitor_index": 0,       # 默认显示器索引
}
